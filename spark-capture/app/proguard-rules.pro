# Add project-specific ProGuard rules here.
# Rules for the wake-word SDK (Phase 3) and any transcription SDK
# (Phase 4) will likely need to be added once those dependencies land —
# native/compiled libraries often need explicit keep rules.
