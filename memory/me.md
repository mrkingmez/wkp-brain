# About the User

**This file is a pointer. Do not write facts about Zac here.**

Single source of truth for who Zac is:

→ `ME.md` at the repo root

It is already pulled in automatically by the root `CLAUDE.md`, so it loads every
session without this file doing anything.

## Why this stays empty on purpose

Both the third-party memory skill and Claude Code's native memory want to keep
their own profile of the user. Two files describing the same person drift apart,
and there is no way to tell which one got read on a given session.

New facts about how Zac works go in `ME.md` — and get flagged out loud the same
turn they come up, never filed silently.
